{
  "name": "erbello-artifact-gallery",
  "version": "46.0.0",
  "private": true,
  "description": "ERBELLO project gallery with blog-style posts, custom categories, manifest ZIP uploads, Storage assets, privacy controls and admin system tools.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/jsx.js && node --check lib/ai-posting.js && node --check public/app.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.9",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "express-rate-limit": "^7.5.1",
    "jszip": "^3.10.1"
  },
  "engines": {
    "node": ">=18"
  }
}
