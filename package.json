{
  "name": "erbello-artifact-gallery",
  "version": "32.0.0",
  "private": true,
  "description": "ERBELLO project gallery with blog-style posts, custom categories, manifest ZIP uploads, Storage assets, privacy controls and admin system tools.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/jsx.js && node --check public/app.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "jszip": "^3.10.1"
  },
  "engines": {
    "node": ">=18"
  }
}
